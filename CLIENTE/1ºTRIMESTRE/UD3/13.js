let arrayNumeros = [];
let arrayPares = [];
let arrayImpares = [];
let arrayFinal = [];
debugger;
/*
function añadir() {
    if (!isNaN(numero)) {
        // Verifica si el valor ingresado es un número
        arrayNumeros.push(numero);
        document.getElementById("numero").value = "";
    } else {
        alert("Por favor, ingresa un número válido.");
    }
}
function calcular() {
  for (let i = 0; i < arrayNumeros.length; i++) {
    if (arrayNumeros[i] % 2 == 0) {
      arrayPares.push(arrayNumeros[i]);
    } else {
      arrayImpares.push(arrayNumeros[i]);
    }
  }
  arrayPares.sort();
  arrayImpares.sort();
  if (arrayNumeros.length % 2 == 0) {
    arrayFinal = arrayPares.concat(arrayImpares);
  } else {
    arrayFinal = arrayImpares.concat(arrayPares);
  }
  document.getElementById("resultado").innerHTML =
    "La lista final ordenada de números es " + arrayFinal.join(",") + "<br>";
}
*/