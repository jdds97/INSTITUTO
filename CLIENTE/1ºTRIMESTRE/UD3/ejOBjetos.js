const persona = { };
persona.nombre = "Juan";
console.log(persona.nombre); // Juan
array.forEach(element => {
    
});
//Para sacar los valores de un array
for in 
objeto 
for of
for (let i in person) {
    if (Object.hasOwnProperty.call(object, i)person   const element = object[person 
    }
}
for (const key in object) {
    if (object.hasOwnProperty(key)) {
        const element = object[key];
        
    }
}
for (const iterator of object) {
    
}